import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Project = (props) => {
    return(
        <View>
            <Text>Project {props.number}</Text>
        </View>
    )
}

export default Project;